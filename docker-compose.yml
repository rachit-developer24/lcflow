services:
  postgres:
    image: postgres:16
    container_name: lcflow-postgres
    environment:
      POSTGRES_DB: lc_issuance
      POSTGRES_USER: lcflow
      POSTGRES_PASSWORD: lcflow_local
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: